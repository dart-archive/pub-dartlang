{{! Copyright (c) 2012, the Dart project authors.  Please see the AUTHORS file
    for details. All rights reserved. Use of this source code is governed by a
    BSD-style license that can be found in the LICENSE file. }}

<div class="row-fluid">
  <div class="span7">
    <h1>{{package.name}}
      <span class="version">{{package.latest_version.version}}</span>
    </h1>
    {{#package.description}}
      <p>{{package.description}}</p>
    {{/package.description}}
    <table>
      <tr>
        <th>Authors</th>
        <td>{{& package.authors_html}}</td>
      </tr>
      {{#package.homepage}}
        <tr>
          <th>Homepage</th>
          <td><a href="{{package.homepage}}">{{package.homepage}}</a></td>
        </tr>
      {{/package.homepage}}
      <tr>
        <th>Uploaders</th>
        <td>{{& package.uploaders_html}}</td>
      </tr>
      <tr>
        <th>Versions</th>
        <td>
          <table>
            <thead>
              <tr>
                <th>Version</th>
                <th>Uploaded</th>
                <th class="archive" width="60">Archive</th>
              </tr>
            </thead>
            {{#versions}}
              <tr>
                <th>{{version}}</th>
                <td>{{short_created}}</td>
                <td class="archive">
                  <a href="{{download_url}}"
                     title="Download {{package.name}} {{version}} archive">
                    <i class="icon-download-alt">
                      Download {{package.name}} {{version}} archive
                    </i>
                  </a>
                </td>
              </tr>
            {{/versions}}
            {{#show_versions_link}}
              <tr>
                <td>
                  <a href="/packages/{{package.name}}/versions">
                    All {{version_count}} versions...
                  </a>
                </td>
              </tr>
            {{/show_versions_link}}
          </table>
        </td>
      </tr>
    </table>
  </div>
  <div class="span5">
    <div class="how-to-use">
      <h3>To use {{package.name}}:</h3>
      <ol>
        <li><p>Add it to your <code>pubspec.yaml</code> file:</p>
<pre>
dependencies:
  <strong>{{package.name}}: {{package.latest_version.example_version_constraint}}</strong>
</pre>
          <p class="application-constraint-note">
            <a href="/doc/glossary.html#application-package">Apps</a> should use
            <strong><code>any</code></strong> as the
            <a href="/doc/glossary.html#version-constraint">version
            constraint</a>.
          </p>
        </li>
        <li></p>Install it
        (<a href="http://www.dartlang.org/docs/editor/">Dart Editor</a>):</p>
<pre>
Menu > Tools > <strong>Pub Install</strong>
</pre>
        <p>Install it (command line):</p>
<pre>
$ <strong>pub install</strong>
</pre>
        </li>
        {{#package.latest_version.has_libraries}}
          <li><p>Import it in your code:</p>
<pre class="highlight">{{#package.latest_version.import_examples}}
<span class="import-example"><span class="k">import</span> <span class="s">'package:<strong>{{package}}</strong>/<wbr/><strong>{{library}}</strong>'</span>;</span>{{/package.latest_version.import_examples}}
</pre>
          </li>
        {{/package.latest_version.has_libraries}}
      </ol>
    </div>
    {{#is_uploader}}
      <p><a class="btn" href="/packages/{{package.name}}/versions/new">
      Upload Version
      </a></p>
    {{/is_uploader}}
  </div>
</div>
