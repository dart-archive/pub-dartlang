<!doctype html5>
<html>
  <head>
    <title>Pub Package Repository</title>
  </head>
  <body>
    <div id="header">
      {{#logged_in}}
        <a href="{{logout_url}}">Log Out</a>
      {{/logged_in}}
      {{^logged_in}}
        <a href="{{login_url}}">Log In</a>
      {{/logged_in}}

      {{#message}}
        <h3>{{message}}</h3>
      {{/message}}
    </div>
    {{& content}}
  </body>
</html>
